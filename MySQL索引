索引

使用索引的目的：
    为了提高查询的效率，就像书的目录一样。

索引的模型：
    1.哈希表
    采用的是拉链法，由于其所存储的值并不是递增的，所以其查询速度很慢，但是其增加速度很快。
    其使用场景为等值查询的场景。

    2.有序数组
    有序数组在等值查询和范围查询场景中的性能都非常优秀。但是需要更新数据就TM麻烦的一比。

    有序数组只适用于静态存储引擎。


    3.搜索树

InnoDB引擎的索引模型为B+树

